export const ruleId = 'services.effect-no-owned-transactions-or-delivery'
export const summary = 'Keep effect services inside use-case-owned transaction boundaries.'
export const family = 'neutral'
export const defaultEnabled = true
export const requiredProjectSurface = null

<span id="services_effect_no_owned_transactions_or_delivery"></span>

## `services.effect-no-owned-transactions-or-delivery`

Keep effect services inside use-case-owned transaction boundaries.

**Status:** Enabled by default.

### Why this exists

Effect services perform focused effects inside a transaction boundary selected by the calling use case, and remain independent from transport code.

### Failing example

```python
from acme.delivery.fastapi.schemas.orders import OrderResponse

class NotificationService(BaseEffectService):
    uow_manager: Injected[OrderUnitOfWorkManager]

    async def send(self) -> OrderEntity:
        await self.uow_manager.commit()
```

### Passing example

```python
class NotificationService(BaseEffectService):
    gateway: NotificationGateway

    async def send(self, message: MessageDTO) -> None:
        await self.gateway.send(message)
```

### Detection notes

Rejects delivery/framework imports, injected UnitOfWorkManager fields, `commit` or `rollback` calls, and method return annotations containing `Entity` in BaseEffectService descendants.
